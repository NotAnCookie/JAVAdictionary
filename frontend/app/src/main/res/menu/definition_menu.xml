<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_favourite_add"
        android:icon="@drawable/ic_action_favourite_empty"
        android:iconTint="?attr/colorOnPrimary"
        android:title="Add favourite"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/action_favourite_remove"
        android:icon="@drawable/ic_action_favourite"
        android:iconTint="?attr/colorOnPrimary"
        android:title="Remove from favourites"
        android:visible="false"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_search"
        android:icon="@android:drawable/ic_menu_search"
        android:iconTint="?attr/colorOnPrimary"
        android:title="Search"
        app:showAsAction="ifRoom" />


</menu>